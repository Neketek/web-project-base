import Base from "../base";
import React from "react";

class ProfileContainer extends Base{
  container({render:{container}}){
    return <div>Profiles</div>;
  }
}

export default ProfileContainer.connect();
